# Vault Configuration
# Local Dev: Use http://127.0.0.1:8200 (port-forwarded)
VAULT_ADDR=

# Auth Method 1: Root/Dev Token (Easiest for local)
VAULT_DEV_TOKEN=""

# Auth Method 2: K8s Service Account Token (Simulate K8s auth locally)
# Run `kubectl create token adminator-sa -n adminator` to get this
K8S_SA_TOKEN=""

# Database Configuration
# Core Info (Non-Sensitive)
# Note: For local dev, port-forward the postgres service
DB_SERVER=
DB_USER=
DB_NAME=
# Sensitive (Moved to Vault: adminator/config)
# Uncomment to seed, then comment out to verify Vault fetch
# DB_PASSWORD=""

# JWT Configuration
# Sensitive (Moved to Vault: adminator/config)
# JWT_SECRET=

# MinIO / S3 Configuration
# Core Info
# Note: For local dev, port-forward the minio service
S3_ENDPOINT=
S3_BUCKET_NAME=
S3_REGION=
# Sensitive (Moved to Vault: adminator/config)
# S3_ACCESS_KEY=
# S3_SECRET_KEY=""

# AWS Credentials (Dev Fallback / Seeding)
# Sensitive (Moved to Vault: apps/tasty-customers/integrations/aws/base)
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
